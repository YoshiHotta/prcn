/*
#include <iostream>
#include <algorithm>
#include <climits>
#include <vector>
#include <stack>
#include <queue>
#include <set>
#include <map>
#include <cmath>

using namespace std;

int main() {
    int a,b,c;
    cin >> a >> b >> c;
    int mx = max({a,b,c});
    if((3 * mx) %2 == (a + b + c) %2) {
        cout  << (3 * mx - a- b- c)/2 << endl;
    } else
        cout  << 1 + (3 * mx+1 - a- b- c)/2 << endl;

    return 0;
}
*/
