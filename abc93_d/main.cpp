/*
#include <iostream>
#include <algorithm>
#include <climits>
#include <vector>
#include <stack>
#include <queue>
#include <set>
#include <map>
typedef long long ll;
using namespace std;

int main() {
    int q;
    cin >> q;
    for (int i = 0; i < q; ++i) {
        ll a,b;
        cin >> a >> b;
        cout << a * b / (b + 1) + (a * b) / (a + 1) << endl;

    }


    return 0;
}
*/
