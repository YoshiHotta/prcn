/*
#include <iostream>
#include <algorithm>
#include <climits>
#include <vector>
#include <stack>
#include <queue>
#include <set>
#include <map>
using namespace std;

int main() {
    long long x, y;
    cin >> x >> y;
    if(x == 1 && y == 1) {
        cout << 1;
    } else if (x == 1 || y == 1) cout << max(x,y) - 2;
    else {
        cout << (x-2) * (y-2) ;
    }
    cout << endl;


    return 0;
}

*/
