/*
#include <iostream>
#include <vector>

using namespace std;

int n;
vector<int> P;

struct Node {
    Node *l;
    Node *r;

};

Node nodes[100];
int nd = -1;
void makeTree(int l, int r) {
    nd++;
    int mid = (l + r) / 2;
    nodes[nd]

}

int main() {
    cin >> n;
    for (int i = 0; i < n; ++i) {
        int v;
        cin >> v;
        P.at(i) = v;
    }
    sort(P.begin(), P.end());

    makeTree(0, n);

    return 0;
}
*/
