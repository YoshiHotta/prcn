/*
#include <iostream>
#include <algorithm>
#include <climits>
#include <vector>
#include <stack>
#include <cmath>
#include <queue>
#include <set>
#include <map>
using namespace std;

int main() {
    int n, l,r;
    cin >> n>> l>>t;
    vector<int> x(n), w(n);
    for (int i = 0; i < n; ++i) {
        cin >> x[i] >> w[i];
    }

    for (int j = 0; j < n; ++j) {
        if(w[j] == 1) {

        }
    }

    return 0;
}
*/
