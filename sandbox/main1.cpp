/*
#include <iostream>
#include <algorithm>
#include <climits>
#include <vector>
#include <stack>
#include <cmath>
#include <queue>
#include <set>
#include <map>
using namespace std;

int main() {
    vector<int> a = {1,2,3,4,5};
    auto it1 = lower_bound(a.begin(), a.end(), -3);
    auto it2 = upper_bound(a.begin(), a.end(), -3);

    auto c = it1 - it2;
    auto p = *it1;
    auto q = *it2;
    int x = 1;

    return 0;
}
*/
