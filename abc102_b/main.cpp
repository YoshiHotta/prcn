/*
#include <iostream>
#include <vector>

using namespace std;

int N ;
vector<int> A;
int main() {
    cin >> N ;
    for (int i = 0; i < N; ++i) {
        int b;
        cin >> b;
        A.push_back(b);
    }
    sort(A.begin(), A.end());
    cout << A.at(N-1) - A.at(0) << endl;

    return 0;
}
*/
